apiVersion: v1
kind: Service
metadata:
  name: {{ include "lichess-bot.fullname" . }}
  labels:
    {{- include "lichess-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lichess-bot.selectorLabels" . | nindent 4 }}
